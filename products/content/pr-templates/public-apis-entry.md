# Public APIs PR Template

## For marcelscruz/public-apis

Add this row to the **Development** category table:

```
| [ToolPipe](https://toolpipe.dev/docs) | 238+ developer utility APIs: JSON, QR, hash, UUID, DNS, PDF, regex, and more | No | Yes | Yes |
```

## For public-apis/public-apis (legacy)

Add this row to the **Development** category:

```
| [ToolPipe](https://toolpipe.dev/docs) | 238+ developer utility APIs including JSON, QR, hash, UUID, DNS, PDF | `No` | Yes | Yes |
```

## For ripienaar/free-for-dev

Add under **APIs, Data, and ML** section:

```
  * [ToolPipe](https://toolpipe.dev) - 238+ free developer utility APIs (JSON formatter, QR code, hash, UUID, DNS, PDF, regex, JWT decoder, SSL checker, and more). Free tier: 100 API calls/day, no signup required.
```

## PR Title
Add ToolPipe - Free Developer Utility APIs

## PR Body
Adding ToolPipe (https://toolpipe.dev), a collection of 238+ free developer utility APIs.

Features:
- JSON formatter, QR code generator, hash generator, UUID generator
- DNS lookup, SSL checker, WHOIS lookup, PDF tools
- Regex tester, JWT decoder, code review, and 200+ more
- Free tier: 100 calls/day, no signup required
- Full API documentation: https://toolpipe.dev/docs
- Also available as an MCP server for AI agents
